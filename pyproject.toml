[project]
name = "fal-api"
description = "Custom nodes for using fal API. Video generation with Kling, Runway, Luma. Image generation with Flux. LLMs and VLMs OpenAI, Claude, Llama and Gemini."
version = "1.0.12"
license = "Apache-2.0"
dependencies = ["fal-client", "torch", "opencv-python", "requests", "numpy", "Pillow"]

[tool.setuptools]
packages = []

[project.urls]
Repository = "https://github.com/ei-grad/ComfyUI-fal-API"

[tool.comfy]
PublisherId = "ei-grad"
DisplayName = "ComfyUI-fal-API"
Icon = ""
